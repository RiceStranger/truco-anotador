extends Node2D

# Spawns coins - UPDATE: the coins were a bad idea
# this is just a leftover. The idea was for the coinds to represent points just like the lights did with normal Truco. However, it made spatial problems. The current method of just displaying numbers is way simpler.
# var scene : PackedScene = preload("res://coin_spawn.tscn")

func _ready() -> void:
	pass


func _process(delta: float) -> void:
	pass
